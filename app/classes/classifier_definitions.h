#ifndef CLASSIFIER_H
#define CLASSIFIER_H

#define NO_SMELL_STRING "No Smell"

enum class InputFunctionType {average, medianAverage, none};
enum class OutputFunctionType {logsoftmax, sigmoid, none};

#endif // CLASSIFIER_H
